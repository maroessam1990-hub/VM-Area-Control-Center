# VM Area Control Center
Android build package for Marwan Essam.

GitHub Actions builds a debug APK automatically. Open Actions > Build Android APK, then download the VM-Area-Control-Center-APK artifact after a successful run.
